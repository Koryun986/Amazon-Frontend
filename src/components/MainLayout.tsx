"use client"
import {Layout} from "antd";
import Header from "./Header";

const MainLayout = () => {
    return (
        <Layout>
            <Header />
        </Layout>
    );
};

export default MainLayout;